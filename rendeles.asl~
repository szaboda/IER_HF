// Agent rendeles in project Etterem.mas2j

/* Initial beliefs and rules */

/* Initial goals */

!start.

/* Plans */

+!start : true <- .random(X); .random(Y); .send(anyag, tell, rendeles(math.round(X*100-0.5),math.round(Y*10-0.5)));
	.print("A(z) ",math.round(X*100-0.5),". vendeg ",math.round(Y*10-0.5),"-t rendelt, szoltam rola anyagnak").
	//a vendeg azonositojat 0 es 99, a valasztasat 0 és 9 kozotti veletlen szammal fejezzuk ki

